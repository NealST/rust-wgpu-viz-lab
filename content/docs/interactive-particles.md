# Interactive Particles

This demo tracks the mouse position and passes it to the compute shader via a **Uniform Buffer**.

## Your Task

Open `crates/viz-core/src/interactive_particles.rs` and update the `COMPUTE_SHADER` WGSL code to add attraction or repulsion forces between particles and the mouse!